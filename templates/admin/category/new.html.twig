{% extends 'admin/admin.html.twig' %}

{% block title "Ajouter une catégorie" %}

{% block body %}
    <h1>Ajout d'une catégorie</h1>

    {{ form_start(form) }}
        <div hidden>
            {{ form_row(form.slug) }}
        </div>
    {{ form_end(form) }}
{% endblock %}
